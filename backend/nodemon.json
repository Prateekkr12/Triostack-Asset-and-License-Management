{
  "watch": ["src"],
  "ext": "ts,json",
  "ignore": ["src/**/*.test.ts", "src/**/*.spec.ts"],
  "exec": "ts-node --transpile-only --files src/server.ts",
  "env": {
    "NODE_ENV": "development"
  }
}
